# Copy to terraform/service-catalog.env and adjust values as needed.

# HCP Terraform organization (required)
export TFC_ORGANIZATION="ECOSYSTEMGLOBALSOLUTIONSINC"

# HCP Terraform hostname (optional; defaults to app.terraform.io)
export TFC_HOSTNAME="app.terraform.io"

# Target AWS region for the engine (required)
export AWS_REGION="us-west-2"

# Optional: AWS CLI profile to use (uncomment to use a specific profile)
# export AWS_PROFILE="default"

# Authentication to HCP Terraform:
# Option A: Export a token in your shell (recommended for CI-less local runs)
# export TF_TOKEN_app_terraform_io="<your-hcp-terraform-token>"

# Option B: Use ~/.terraformrc with credentials:
# credentials "app.terraform.io" {
#   token = "<your-hcp-terraform-token>"
# }

# NOTE:
# - Do NOT commit real tokens. Keep this file as an example only.
# - The setup script will use these exports during 'terraform init/apply' of the engine.
